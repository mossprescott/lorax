(use 'meta.core)

(make-node :core/program [
  (make-node :core/doc { :string "Examples using the core syntax..."})
   
  (make-node :core/comment {
    :expr
    (make-node :core/fraction {
      :num
      (make-node :kernel/int { :value 1 })

      :denom
      (make-node :kernel/int { :value 0 })
    })
  })
  
  (make-node :core/not {
    :expr
    (make-node :kernel/true)
  })
  
  (make-node :core/unaryminus {
    :expr
    (make-node :kernel/int { :value 137 })
  })

  (make-node :core/sqrt {
    :expr
    (make-node :kernel/int { :value 4 })
  })
  
  (make-node :core/sqrt {
    :expr
    (make-node :core/times {
      :left
      (make-node :kernel/int { :value 4 })
      :right
      (make-node :kernel/int { :value 9 })
    })
  })
  
  (make-node :core/times {
    :left
    (make-node :core/fraction {
      :num
      (make-node :core/plus {
        :left
        (make-node :kernel/int { :value 1 })
        :right
        (make-node :kernel/int { :value 2 })
      })
      :denom
      (make-node :kernel/int { :value 4 })
    })
    
    :right
    (make-node :kernel/int { :value 100 })
  })
  
  ; (make-node :core/square {
    ; :expr
    (make-node :core/times {
      :left
      (make-node :core/fraction {
        :num
        (make-node :kernel/int { :value 1 })

        :denom
        (make-node :kernel/int { :value 3 })
      })
      
      :right
      (make-node :core/sqrt {
        :expr
        (make-node :core/square {
          :expr
          (make-node :kernel/int { :value 3 })
        })
      })
    })
  ; })
  
  (make-node :core/where {
    :body 
    (make-node :core/plus {
      :left
      (make-node :kernel/var {
        :ref (ref-node :a_where)
      })
      
      :right
      (make-node :kernel/var {
        :ref (ref-node :b_where)
      })
    })
    
    :vars
    (make-node :core/whereVars [
      (make-node :core/whereVar {
        :bind
        (make-node :kernel/bind :a_where { :name "a" })
        
        :expr
        (make-node :kernel/int { :value 1 })
      })
      (make-node :core/whereVar {
        :bind
        (make-node :kernel/bind :b_where { :name "b" })
        
        :expr
        (make-node :kernel/int { :value 2 })
      })
    ])
  })

  ; (make-node :core/and {
  ;   :left
  ;   (make-node :kernel/true)
  ;   
  ;   :right
  ;   (make-node :kernel/false)
  ; })
  ; 
  ; (node :core/greater
  ;   :left
  ;   (node :core/times
  ;     :left 
  ;     (node :kernel/int :value 11)
  ;     
  ;     :right
  ;     (node :kernel/int :value 4))
  ;   
  ;   :right
  ;   (node :kernel/int :value 42))
  ; 
  ; (node :core/factorial
  ;   :expr
  ;   (node :kernel/int :value 4))
  ; 
  ; (node :core/factorial
  ;   :expr
  ;   (node :core/times
  ;     :left
  ;     (node :kernel/int :value 2)
  ;     
  ;     :right
  ;     (node :kernel/int :value 10)))
  ])
