(use 'meta.core)

(make-node :core/program [
  ; for
  (make-node :core/for {
    :expr
    (make-node :core/square {
      :expr
      (make-node :kernel/var {
        :ref (ref-node :x_for)
      })
    })

    :x
    (make-node :kernel/bind :x_for { :name "x" })
    
    :seq
    (make-node :core/range {
      :min
      (make-node :view/expr/missing)
  
      :max
      (make-node :view/expr/missing)
    })    
  })
])
